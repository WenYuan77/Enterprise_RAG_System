# ==============================================================================
# RAG ENTERPRISE - Network Configuration
# ==============================================================================
# Copy this file to `.env` and modify values based on your environment

# ------------------------------------------------------------------------------
# FRONTEND API URL CONFIGURATION
# ------------------------------------------------------------------------------

# SCENARIO 1: LOCALHOST (default - development)
# Access only from this computer
#VITE_API_URL=http://localhost:8000

# SCENARIO 2: LOCAL NETWORK (LAN)
# Access from devices on your local network
# Replace 192.168.1.100 with the IP of the server running Docker
# Find your IP with: ip addr show | grep inet
VITE_API_URL=http://192.168.1.165:8000

# SCENARIO 3: INTERNET (with domain and reverse proxy)
# Public access from internet
# Requires: domain, Nginx reverse proxy, SSL (recommended)
# Example: VITE_API_URL=https://rag.yourdomain.com
#VITE_API_URL=https://yourdomain.com

# ------------------------------------------------------------------------------
# BACKEND PORT (don't modify unless you changed docker-compose)
# ------------------------------------------------------------------------------
BACKEND_PORT=8000
FRONTEND_PORT=3000

# ------------------------------------------------------------------------------
# SECURITY (for production) - IMPORTANT!
# ------------------------------------------------------------------------------
# These settings are OPTIONAL but STRONGLY RECOMMENDED for production deployments

# JWT Secret Key - Used to sign authentication tokens
# Generate with: openssl rand -hex 32
# If not set, backend uses an insecure default key (WARNING will be logged)
# JWT_SECRET_KEY=your-secret-key-here

# Allowed Origins - CORS protection (comma-separated list)
# Only requests from these domains will be allowed to access the API
# If not set, ALL origins are allowed (insecure - development only)
# Examples:
#   ALLOWED_ORIGINS=https://yourdomain.com                          # Single domain
#   ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com  # Multiple domains
#   ALLOWED_ORIGINS=http://localhost:3000,http://192.168.1.100:3000    # Local + LAN
# ALLOWED_ORIGINS=https://yourdomain.com

# ------------------------------------------------------------------------------
# PRODUCTION EXAMPLE (with reverse proxy)
# ------------------------------------------------------------------------------
# If you have a domain with reverse proxy (e.g., https://rag.yourdomain.com):
#
# VITE_API_URL=https://rag.yourdomain.com
# JWT_SECRET_KEY=<generate-with-openssl-rand-hex-32>
# ALLOWED_ORIGINS=https://rag.yourdomain.com
#
# Your reverse proxy should forward requests to the backend container:
#   https://rag.yourdomain.com -> http://backend-ip:8000
#
# Then restart backend: docker compose restart backend
