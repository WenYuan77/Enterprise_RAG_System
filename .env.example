# ==============================================================================
# RAG ENTERPRISE - Network Configuration
# ==============================================================================
# Copy this file to `.env` and modify the values according to your environment

# ------------------------------------------------------------------------------
# SCENARIO 1: LOCALHOST (default - development)
# ------------------------------------------------------------------------------
# Access only from this computer
# VITE_API_URL=http://localhost:8000

# ------------------------------------------------------------------------------
# SCENARIO 2: LOCAL NETWORK (LAN)
# ------------------------------------------------------------------------------
# Access from devices on your local network
# Replace 192.168.1.100 with the IP of the server running Docker
# Find your IP with: ip addr show | grep inet
# VITE_API_URL=http://192.168.1.100:8000

# ------------------------------------------------------------------------------
# SCENARIO 3: INTERNET (with domain and reverse proxy)
# ------------------------------------------------------------------------------
# Public access from the internet
# Requires: domain, Nginx reverse proxy, SSL (optional but recommended)
# VITE_API_URL=https://yourdomain.com/api

# ------------------------------------------------------------------------------
# ACTIVE CONFIGURATION (uncomment ONE line only)
# ------------------------------------------------------------------------------
VITE_API_URL=http://localhost:8000

# ------------------------------------------------------------------------------
# BACKEND PORT (do not modify unless you changed docker-compose)
# ------------------------------------------------------------------------------
BACKEND_PORT=8000
FRONTEND_PORT=3000

# ------------------------------------------------------------------------------
# SECURITY (for production)
# ------------------------------------------------------------------------------
# JWT_SECRET_KEY=your-super-secret-key-change-this-in-production
# ALLOWED_ORIGINS=http://localhost:3000,http://192.168.1.100:3000
