# ==============================================================================
# RAG ENTERPRISE - Network Configuration
# ==============================================================================
# Copia questo file in `.env` e modifica i valori in base al tuo ambiente

# ------------------------------------------------------------------------------
# SCENARIO 1: LOCALHOST (default - sviluppo)
# ------------------------------------------------------------------------------
# Accesso solo da questo computer
# REACT_APP_API_URL=http://localhost:8000

# ------------------------------------------------------------------------------
# SCENARIO 2: RETE LOCALE (LAN)
# ------------------------------------------------------------------------------
# Accesso da dispositivi nella tua rete locale
# Sostituisci 192.168.1.100 con l'IP del server dove gira Docker
# Trova il tuo IP con: ip addr show | grep inet
# REACT_APP_API_URL=http://192.168.1.100:8000

# ------------------------------------------------------------------------------
# SCENARIO 3: INTERNET (con dominio e reverse proxy)
# ------------------------------------------------------------------------------
# Accesso pubblico da internet
# Richiede: dominio, Nginx reverse proxy, SSL (opzionale ma raccomandato)
# REACT_APP_API_URL=https://tuodominio.com/api

# ------------------------------------------------------------------------------
# CONFIGURAZIONE ATTIVA (decommentare UNA sola linea)
# ------------------------------------------------------------------------------
REACT_APP_API_URL=http://localhost:8000

# ------------------------------------------------------------------------------
# BACKEND PORT (non modificare a meno che tu non abbia cambiato docker-compose)
# ------------------------------------------------------------------------------
BACKEND_PORT=8000
FRONTEND_PORT=3000

# ------------------------------------------------------------------------------
# SECURITY (per produzione)
# ------------------------------------------------------------------------------
# JWT_SECRET_KEY=your-super-secret-key-change-this-in-production
# ALLOWED_ORIGINS=http://localhost:3000,http://192.168.1.100:3000
